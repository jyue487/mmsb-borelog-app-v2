# Copy to .env.local and fill in. Values are read by babel-preset-expo at build time
# and are embedded in the shipped bundle, so none of them may be a secret.
# Cloud builds do NOT read this file - see `eas env:list`.

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_KEY=
EXPO_PUBLIC_POWERSYNC_URL=
